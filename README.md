# 吵架急救助手（独立网页）

从情侣小账本中拆分出的独立模块，聚焦「先冷静 -> 再沟通 -> 甜甜收尾」。

## 本地使用

- 直接打开 `index.html`。
- 或使用任意静态服务器启动。

## 部署

推送到 GitHub 后，在仓库 `Settings -> Pages`：

- Source: `Deploy from a branch`
- Branch: `main`
- Folder: `/ (root)`

页面地址会是：`https://<username>.github.io/<repo>/`
